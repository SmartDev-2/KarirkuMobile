<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.KARIRKU" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Warna utama aplikasi bisa diset di sini -->
        <!-- <item name="colorPrimary">@color/blue</item> -->
    </style>

    <!-- App theme utama -->
    <style name="Theme.KARIRKU" parent="Base.Theme.KARIRKU">
        <!-- Hapus item berikut karena tidak efektif untuk BottomNavigationView -->
    </style>

    <!-- TAMBAHKAN INI UNTUK HILANGKAN RIPPLE BOTTOM NAVIGATION -->
    <style name="NoRippleBottomNavigation" parent="Widget.Material3.BottomNavigationView">
        <item name="itemRippleColor">@android:color/transparent</item>
        <item name="itemBackground">@android:color/transparent</item>
    </style>

</resources>